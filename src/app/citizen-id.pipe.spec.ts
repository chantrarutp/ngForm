import { CitizenIDPipe } from './citizen-id.pipe';

describe('CitizenIDPipe', () => {
  it('create an instance', () => {
    const pipe = new CitizenIDPipe();
    expect(pipe).toBeTruthy();
  });
});
